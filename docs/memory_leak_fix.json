{
  "error_keywords": ["memory", "leak", "application", "crash"],
  "description": "Application crashes due to a memory leak.",
  "fix": [
    "Use a memory profiling tool to identify leaks.",
    "Check for unclosed resources like file handles or database connections.",
    "Ensure large objects are dereferenced when no longer needed.",
    "Consider implementing garbage collection if not automatic."
  ]
}
